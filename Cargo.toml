[package]
name = "ckan_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy", branch = "main", version = "0.19.0-dev", default-features = false, features = [
  "2d",
  "ui",
  "experimental_bevy_feathers",
] }
